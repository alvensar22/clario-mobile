# =============================================================================
# clario-mobile environment variables
# =============================================================================
# Copy this file to .env and fill in values. Do not commit .env to git.
# Expo reads EXPO_PUBLIC_* at build time (expo start / prebuild).
# =============================================================================

# -----------------------------------------------------------------------------
# API (required)
# -----------------------------------------------------------------------------
# Base URL of the clario-web backend. Mobile app calls this for auth, feed,
# posts, premium checkout/portal, etc.
# - Local: run clario-web (e.g. next dev) and use its URL.
# - Production: your deployed clario-web URL.
EXPO_PUBLIC_API_URL=http://localhost:3000

# =============================================================================
# Premium / Stripe
# =============================================================================
# Stripe (checkout, webhooks, billing portal) is configured on the WEB project
# (clario-web), not in this mobile app. The mobile app only calls:
#   POST /api/checkout   (body: { plan: 'monthly' | 'annual' })
#   POST /api/premium/portal
# So you do NOT put Stripe keys or webhook secrets here.
# Ensure clario-web has the correct env (STRIPE_SECRET_KEY, price IDs,
# STRIPE_WEBHOOK_SECRET, NEXT_PUBLIC_BASE_URL, etc.) and that
# EXPO_PUBLIC_API_URL above points to that running backend.
# See clario-web STRIPE_SETUP.md (or docs) for full Stripe setup.
# =============================================================================
